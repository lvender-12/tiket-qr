<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/profile.css">
    <title>Document</title>
</head>
<body>
    <div class="profile-container">
        <a href="/logout" class="logout-link">Logout</a>
        <div class="profile-header">
            <img src="bendera-indonesia.png" alt="Bendera Indonesia" class="bendera">
            <h1 class="welcome-text">Selamat Hari Kemerdekaan Indonesia!</h1>
            <p class="sub-text">Merdeka! Tanah Airku!</p>
        </div>
        <div class="profile-details">
            <img src="/qr/<%= user.qr %>" alt="QR Code">
            <p class="detail">Email: <span class="email"><%= user.email %></span></p>
            <p class="detail">Role: <span class="role"><%= user.role %></span></p>
            <% if (user.hadir == null || user.hadir == belum) { %>
                <p class="detail">Hadir: <span class="attendance">belum</span></p>
            <% } else { %>
                <p class="detail">Hadir: <span class="attendance"><%= user.hadir %></span></p>
            <% } %>
            <% if (user.makanSiang == null || user.makanSiang == belum) { %>
                <p class="detail">Makan Siang: <span class="lunch">belum</span></p>
            <% } else { %>
                <p class="detail">Makan Siang: <span class="lunch"><%= user.makanSiang %></span></p>
            <% } %>
            <% if (user.Snack == null || user.snack == belum) { %>
                <p class="detail">Snack: <span class="snack"><%= user.snack %></span></p>
            <% } else { %>
                <p class="detail">Snack: <span class="snack"><%= user.snack %></span></p>
            <% } %>
        </div>
    </div>
    <script src="/js/profile.js"></script>
</body>
</html>
